{{/* Make sure all variables are set properly */}}
{{- include "tc.v1.common.loader.init" . }}

{{- define "cert-manager-webhook-pdns.secretVolumes" -}}
enabled: true
mountPath: "/secrets"
readOnly: true
type: "secret"
objectName: {{ include "cert-manager-webhook-pdns.servingCertificate" . }}
{{- end -}}

{{- $_ := set .Values.persistence "creds" (include "cert-manager-webhook-pdns.secretVolumes" . | fromYaml) -}}

{{/* Render the templates */}}
{{ include "tc.v1.common.loader.apply" . }}
